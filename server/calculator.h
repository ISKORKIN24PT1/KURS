/**
 * @file calculator.h
 * @author Искоркин Андрей Дмитриевич
 * @date 08.12.2025
 * @brief Заголовочный файл класса Calculator для вычисления суммы квадратов элементов вектора.
 * @details Класс реализует основную вычислительную операцию системы.
 * @warning Выполняет контроль переполнения при вычислениях.
 */

#ifndef CALCULATOR_H
#define CALCULATOR_H

#include <vector>
#include <cstdint>

/**
 * @brief Класс для выполнения вычислений над векторами данных.
 * 
 * @details Класс выполняет основную вычислительную операцию системы:
 * вычисление суммы квадратов элементов вектора с контролем переполнения.
 * Предназначен для использования в серверной части системы.
 */
class Calculator {
public:
    /**
     * @brief Вычисляет сумму квадратов элементов вектора.
     * @param[in] vector Вектор целых чисел типа uint64_t.
     * @return Сумма квадратов элементов вектора.
     * @throw std::overflow_error если происходит переполнение при вычислении.
     * @details Алгоритм контролирует переполнение как при умножении (квадрат),
     * так и при сложении промежуточных результатов. В случае переполнения
     * результат возвращается как 1 (согласно ТЗ).
     * @warning Все элементы вектора интерпретируются как беззнаковые 64-битные числа.
     */
    uint64_t computeSumOfSquares(const std::vector<uint64_t>& vector);
};

#endif
